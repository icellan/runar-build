---
interface Props {
  class?: string;
}

const { class: className = '' } = Astro.props;
---

<a href="/" class:list={['inline-flex items-center gap-2 font-mono font-bold text-xl tracking-tight text-text hover:text-accent-500 transition-colors', className]}>
  <span>R<span class="text-accent-500">ú</span>nar</span>
</a>
